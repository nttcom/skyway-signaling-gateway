"use strict";

// Connverter/Janus.js

class JanusConverter {
  constructor(){
  }

  tocgof(janus_mesg) {
    var cgof_mesg = {};
    // convert into CGOF format

    return cgof_mesg;
  }

  toserver(cgof_mesg) {
    var janus_mesg = {}

    // convert to janus format
    //
    return janus_mesg;
  }
}

module.exports = JanusConverter;
