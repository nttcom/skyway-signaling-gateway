"use strict";

// Connector/Janus.js

class JanusConnector {
  constructor(){
  }

  connect(callback){
    // start long polling
  }

  send(mesg) {
    // send to janus gateway server
  }
}

module.exports = JanusConnector;
